/*
- gridcombocolumn bound to DA that reads list of categories from DB
- then, link col to grid property name

- three layers app: Data Access Layer, Buissines Logic Layer, UI layer
    - DAL: ADO logic
    - BLL: entity, entity list, entity manager
        - entities store data only
        - entity manager contains functional logic (static, no state)
        - base class for all because there will be common logic
        
*/