@model IEnumerable<TraineesITI.Areas.TrackCourses.Models.TrackCourse>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<a asp-action="Details" asp-controller="Tracks"
   asp-route-id="@ViewBag.Track.Id">
    <h3>@ViewBag.Track.Name</h3>
</a>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course.Topic)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
